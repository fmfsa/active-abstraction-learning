experiment:
  budget: 20
  seeds: 3
  initial_samples: 5
  n_acquire: 1
  epochs: 100
  initial_condition:
    type: fixed
    value: 0.01 # Start with 1% infected
  families: [lodernn]
  methods: [random, uncertainty_sampling, bald]

model:
  dropout_prob: 0.1

acquisition:
  pool_size: 20
  mc_samples: 20

simulation:

